import { BootstrapButtonDirective } from './bootstrap-button.directive';

describe('BootstrapButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BootstrapButtonDirective();
    expect(directive).toBeTruthy();
  });
});
